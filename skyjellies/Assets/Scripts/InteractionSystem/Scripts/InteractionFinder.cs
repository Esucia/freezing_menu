using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

/// <summary>
/// Currently a stub. Intended functionality:
/// Finds interactable components that the player is looking at and gets the closest one
/// Informs interactable component when its first being looked at and stopped being looked at
/// Sends out event when closest interactable changes 
/// </summary>

public class InteractionFinder : MonoBehaviour
{

    void Start()
    {
        
    }

    void Update()
    {
        
    }
}
